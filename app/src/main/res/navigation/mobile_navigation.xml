<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="aston.carlocator.app.ui.addlocator.AddLocatorFragment"
        android:label="@string/add_locator"
        tools:layout="@layout/fragment_add_locator" />

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="aston.carlocator.app.ui.locators.locatorsList"
        android:label="@string/locators"
        tools:layout="@layout/fragment_locators" />
</navigation>